import styled from '@emotion/styled'
/* Const */
import { COLOR, SIZE } from '@/const/index'

export const Wrapper = styled.h4`
  margin: ${SIZE.SPACE.X40} 0 ${SIZE.SPACE.X28};
  padding: ${SIZE.SPACE.X4} ${SIZE.SPACE.X16};
  color: ${COLOR.GRAY_SCALE.GRAY.TEXT_1};
  background-color: ${COLOR.GRAY_SCALE.GRAY.H4};
  border-left: 4px solid ${COLOR.BLUE_GROUP.MAIN};
`
